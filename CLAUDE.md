# CLAUDE.md

## Workflow Rules

- `cargo fmt` and `cargo clippy` before running tests.
- Print a short commit message when work is done.
- Keep this file accurate after any feature/module/command/behavior change.
- **Adding a datetime format:** give it the same coverage as existing ones — locale vocabulary/regex builder (`locale/`, English in `locale/english.rs`), `parser.rs` extraction, `EventInfo::normalize_time` (canonical output), `scheduler.rs` (if math changes), the **Datetime formats** section here, unit tests, and USER/SYSTEM rows in `test-cases.md`. If it conflicts/overlaps with an existing format, stop and propose resolutions instead of silently changing behavior.
- **All user-facing time vocabulary goes through `LocaleProvider`.** Never hardcode a parsed/emitted time word in `parser.rs`/`types.rs`/`view/`. Exception: DB serialization uses the fixed canonical helpers in `types.rs`.

## Project Overview

Perbot is a Telegram reminder bot in Rust (edition 2024). A message leads with a natural-language time expression (`13:30 call the office`); the bot parses it, persists the event to SQLite, and fires the reminder when due. Active events reload on restart.

## Build & Test

```bash
cargo build [--release]
cargo test                 # parser/storage/scheduler/table tests
cargo test <name>          # single test/module, e.g. parser::tests
cargo run --bin bench      # storage benchmark (1000 events)
./docker-build.sh          # two-stage image: static musl build (rust:1-alpine) → alpine runtime
./docker-run.sh            # run container; bind-mounts ./data (perbot.db) and ./logs, reads perbot.env
./deploy.sh dev|prod       # spot deploy (spot.yml): build image locally, ship over SSH, restart on the target
```

Docker: the bot opens `data/perbot.db` relative to its working directory (creating `data/` if missing); `WORKDIR /` keeps the db in the mounted `./data`; logs go to `LOG_DIR=/logs` (mounted `./logs`). `docker-run.sh` requires `TG_BOT_TOKEN`/`TG_ADMIN_ID` (env or `perbot.env`), forwards `RUST_LOG`/`TZ`, runs as the host user so the bind mounts stay writable, and bind-mounts host `/tmp` so the `/tmp/perbot.lock` instance lock is shared with the host (a second instance anywhere on the machine is refused). Restart policy is `on-failure`: crashes restart (with backoff), but a clean exit — `/exit yes` calls `process::exit(0)` — keeps the container down; `chrono-tz` bundles the IANA tzdata into the binary (nothing needed in the alpine image); the `TZ` env var no longer affects scheduling (the image defaults `TZ=Europe/Berlin`) — all scheduling runs in UTC + the per-chat timezone setting.

Deploy: `./deploy.sh dev|prod` runs the `spot.yml` playbook ([umputun/spot](https://github.com/umputun/spot); prod asks for confirmation; extra flags forward to `spot`, e.g. `./deploy.sh dev --dry`). It builds the image locally, `docker save`s it, uploads the tarball + `docker-run.sh` + `backup.sh` (archives `/home/perbot` to `/home/backup/dropbox/perbot`; run manually or via cron on the target) to `/home/perbot/` on the target (sftp copy paths are absolute — sftp does no `~` expansion), `docker load`s and restarts via `docker-run.sh` there, then waits for the container to report running. The placeholder hosts/users in `spot.yml` must be filled in once; `/home/perbot/perbot.env` must already exist on each server (checked, never uploaded — it's gitignored) and the remote user needs `docker`-group access (no sudo anywhere).

## Environment Variables

`TG_BOT_TOKEN` (bot API token, required), `TG_ADMIN_ID` (admin chat ID, i64, required), `RUST_LOG` (`flexi_logger` level), `LOG_DIR` (default `logs`).

## Key Invariants

- **Everything is HTML.** `EventInfo.message` is an HTML fragment (user's Telegram formatting preserved as tags; plain text escaped). All message-bearing output is `ParseMode::Html`; the only MarkdownV2 site is `TgBot::send_markdown` (`/exit` rejection). Only `< > &` need escaping (`teloxide::utils::html::escape`). Conversion happens at ingestion: `main` via `richtext::render_html`, `converter` escapes legacy text; snoozed children store `''` and every event SELECT resolves their message from the parent.
- **All outbound Telegram calls go through `TgBot`** (`tgbot.rs`), never the raw teloxide `Bot` (constructed only to clone into `TgBot::new` and for the dispatcher's updater). The wrapper owns the per-call `ParseMode` and logs payload size + chat/callback/file id on every call.
- **`TgMessage` is opaque to the sender**: final `text` (HTML) + optional `reply_markup`, forwarded verbatim by the sender task in `main`. Producers decide *which* view builder to call; **all composed message text and inline keyboards live only in `view/`** — `state.rs`, `commands/*`, `main.rs`, `pending.rs` never build message HTML or `InlineKeyboardMarkup` themselves (fixed literal one-liners like toasts and admin diagnostics stay at the call site).
- **List reads page in SQL** (`LIMIT`/`OFFSET` + paired `count_…` queries); a chat's full list is never loaded whole. `PageRequest` (`page`/`size`) in → `PageResponse` (`events` + `total`) out, count + page fetched under one provider lock.
- **Instants are UTC, wall-clock rules are chat-local.** Every stored/compared instant (`next_datetime`, `last_next_datetime`, `created_at`, `missed_at`, settings timestamps) is naive **UTC**; the parsed wall-clock fields (`time`/`date`/`days`/`bare_hour`/`monthly_pattern`/`years`) keep chat-local semantics. UTC↔chat-local conversion happens in exactly two places: inside `scheduler::calc_next_at` (math runs in the chat's wall-clock frame, so `every day` keeps its local time across DST) and at the `view/` formatting boundary — both through `tz.rs`, which owns the DST policy (spring-forward gap → forward to the gap end; fall-back fold → earliest offset). The per-chat timezone lives in `settings` under `tz::TIMEZONE_SETTING`; **a chat without one gets the region picker for every message** — `main`'s gate (before all message handling, commands included; plus the edit-entry gate in `commands/event.rs`) replies with the picker instead, while the callback-driven dismiss and snooze still work (display paths fall back to UTC via `EventProvider::tz_or_utc`). Changing timezone re-anchors the chat's active events wall-clock-preserving (`set_timezone`); pure-instant `in_offset` events keep their moment.
- **Time/recurrence fields are mutually exclusive** — a message populates whichever one matched (see EventInfo table).
- **`normalize_time(loc)` is canonical and re-parseable** (idempotent): parses back to the same event under the same locale. Drives the edit/round-trip flow.
- **Localization is threaded, not global.** Every time-format input (parse) and time-bearing output (canonical string, recurrence description, relative time, date pattern, `Next launches:`, the `Time:` label) flows through an explicit `&dyn LocaleProvider`; `locale::for_chat(chat_id)` resolves it (English for every chat today). DB serialization is deliberately **not** localized — `storage.rs`/`converter.rs` persist weekday/unit strings via the fixed canonical free functions in `types.rs` (`day_to_str`, `parse_days`, `unit_from_str`, `TimeUnit::label`), which the English locale reuses.
- **Callback envelope** for event actions: `eid:<id>:<action>:<args>`. Every handler acting on an id **access-checks** `event.chat_id` against the pressing chat (callback ids are user-influenceable). The timezone picker uses `tz:r` / `tz:g:<Region>:<page>` / `tz:p:<Zone>`, the Settings menu `st:o`/`st:s`/`st:on`/`st:off`/`st:t`/`st:h:<H>` (both apply to the pressed message's chat — same authority model as list pagination). All callback data must stay ≤ Telegram's 64-byte limit; a test in `view/timezone.rs` pins it across the whole IANA table.
- **Pending flows are in-memory only** (restart drops them).
- **Chats must register before their events fire.** A chat's first-ever message sets the `activated=true` setting (`EventProvider::activate_chat` in `main`, before the timezone gate) and notifies the admin (`view::user_registered_message`; skipped for the admin's own chat). Activation only happens when the setting is **absent**: an existing `activated=false` (admin deactivation, set manually) is never overwritten — a deactivated chat can't re-activate itself by messaging. The three firing-path queries (`get_next_event`, `get_missed_events`, `get_events_at`) only see activated chats — events of never-registered chats (e.g. admin-imported ones) sit untouched until activation, then fire/reschedule normally (a past-due backlog fires immediately). Interactive per-chat queries stay unfiltered.

## Modules

`lib.rs` re-exports all modules. Shared types live in `types.rs`.

- **locale/** — Localization seam (`mod.rs` + `english.rs`). `LocaleProvider` threads all time vocabulary/regexes/format patterns; `locale::EN`/`for_chat` resolve the active locale. **Adding a locale = supplying data:** fill a `GrammarVocab` and call `TimeGrammar::build` (the shared builder owns the regex shapes), map words to the shared enums, provide output vocabulary/patterns/relative-time. A byte-identity test pins the built English regexes to the historical strings. User-facing name helpers (`weekday_full`/`ordinal_word`/`ordinal_suffix`/`weekday_abbrev_cap`) and output labels (`next_launches_header`/`time_label`) live here.
- **types.rs** — `EventInfo`, `MessageInfo`, `ChatInfo`/`ChatType` (`ChatInfo::from_chat` maps a teloxide `Chat`), `TgMessage`, `MessageSender`, `PageRequest`/`PageResponse`, time enums (`TimeUnit`/`Repetition`/`Ordinal`/`MonthlyPattern`), `NextSource`. The fixed canonical (storage) helpers live only here: `day_to_str`, `parse_days`, `unit_from_str`, `TimeUnit::label`, `NextSource::as_str`/`from_str`. `EventInfo::normalize_time(loc)` pulls all vocabulary from `loc`. `format_time_left` (log-only, not localized) renders a countdown as `1h 2m 3s` for the "Next event"/`update_schedule` log lines.
- **parser.rs** — Stateless extraction over the locale's regexes/word maps. `parse` / `parse_full` (also returns surviving body byte-ranges for `richtext`) / `parse_time_only` (time present, body empty → main's "send me the text" flow) take the chat `Tz`; the `_at` variants take an explicit chat-local `today: NaiveDate` instead (deterministic tests) — it only drives short-date year inference. `created_at` is stamped in UTC. Clock time matches anywhere; offset/bare-hour/short-date must lead. Standalone 4-digit token in 2000..=2100 is a year restriction. Body derived via `richtext::normalize`; `main` overwrites it with the HTML render before persisting.
- **richtext.rs** — Pure. `normalize` is the single source of truth for body normalization: collapses intra-line whitespace, **preserves line breaks verbatim**, tracks each char's source byte offset. `render_html` rebuilds `MessageEntity`s (UTF-16) over the leftover text via teloxide's `Renderer`; falls back to `html::escape`.
- **scheduler.rs** — Pure datetime math. `calc_next[_at](EventInfo[, now_utc], tz)` set `active` + `next_datetime` + `source` (`None` when inactive). `calc_next_at` is the scheduling-side UTC↔local seam: it moves `now` and the repetition base into the chat's wall-clock frame, runs the (unchanged, frame-agnostic) recurrence math there, and converts the result back to UTC via `tz::to_utc` — so day/week/month intervals preserve local time across DST, while an hour/minute interval stretches/shrinks by the offset delta once per transition.
- **tz.rs** — The timezone seam: `TIMEZONE_SETTING` (settings key, IANA name), `parse_tz`, `to_local`/`to_utc` (the only UTC↔chat-local converters; `to_utc` owns the DST policy — gap → forward to gap end, fold → earliest offset), `shift_wallclock` (re-anchor an instant so its wall-clock reading survives a zone change).
- **error.rs** — Crate error (`thiserror`) + `Result<T>`. Libraries use it; binaries wrap with `anyhow`.
- **storage.rs** — `EventStorage` over rusqlite. Tables `chats`, `messages`, `events` (`msg_id` NOT NULL FK; `legacy` flag; nullable `parent` self-FK `ON DELETE CASCADE` marks snoozed events — every event SELECT goes through the shared `event_cols`/`EVENT_FROM` fragments, whose self-join resolves `message` from the parent via `COALESCE`; `last_next_datetime`; `source` TEXT = `NextSource::as_str()`), `missed_events` — the startup missed snapshot (autoincrement `id` preserves missed order; `event_id` UNIQUE FK `ON DELETE CASCADE`; `missed_at` = the datetime the event should have fired at, captured before rescheduling; cleared and repopulated every start), and `settings` — per-chat key/value config (PK `(chat_id, name)`; `set_setting` upserts, bumping `updated_at`; `get_setting` → `Option<String>`). `get_missed_snapshot_by_chat` returns events with `missed_at` substituted into `next_datetime` (display-only — the missed list shows the missed moment, not the post-reschedule state). CRUD + paged active/range/missed-snapshot queries (`get/count_active_by_chat_in_range` take UTC `NaiveDateTime` bounds — callers convert chat-local day windows), `update_schedule` (persists a reschedule incl. source), `backup_to` (`VACUUM INTO`). `get_missed_events(now, limit)` batches the missed backlog with no offset — rescheduling a batch removes it from the predicate. `ACTIVATED_SETTING` (`activated`) + the `ACTIVATED_ONLY` fragment restrict the firing-path queries (`get_next_event`/`get_missed_events`/`get_events_at`) to activated chats. `LAST_SNOOZE_SETTING` (`last_snooze`) holds a chat's last-picked snooze duration as decimal minutes. Morning digest: `DIGEST_SETTING` (`morning_digest`) holds the chat-local send time `"HH:MM"` or `DIGEST_OFF` (`"off"`; absent = off, the default), `DIGEST_SENT_SETTING` (`morning_digest_sent`) the last chat-local date handled; `get_digest_chats()` lists `(chat_id, value)` of enabled **activated** chats. All datetime columns hold naive-UTC text (`%Y-%m-%d %H:%M:%S`, compared lexically).
- **state.rs** — `EventProvider`: `Clone` handle over `Arc<Mutex<_>>` (storage + cached next event). **Storage-backed methods return `crate::error::Result`** and `?`-bubble DB errors; only `get_next_event` (in-memory cache) is infallible. `start(msg_tx)`: clears the `missed_events` table, `move_missed_events` records + reschedules the missed backlog in `MISSED_MOVE_BATCH`-sized batches (never whole in memory), sends each chat from `get_missed_chat_ids` page 0 via `get_missed_snapshot_events` (the same call backing the `ms:<page>` page-turn callbacks), then spawns a 1s poll thread that fires due events and, once per minute, sends due **morning digests**. A **startup** DB error returns `Err` before the thread spawns so `main` can abort; the detached thread logs per-tick errors in place. Fired messages are built by `view::fired_message`; the poll loop predicts the post-fire `active`/`source` with `calc_next_at` (before `update_and_reload` persists it) and hands the flags in so the dismiss row matches the state the buttons will act on. Mutations: `insert_event_and_get[_at]`, `insert_prebuilt_event` (no re-scheduling; importer + snooze), `update_event_and_get[_at]` (edit flow), `delete`, `dismiss(id, chat_id) -> DismissOutcome` (access-checks, reschedules at `next_datetime + 1s`; returns updated event / `Inactive` / `NotFound`), `dismiss_repetition` (steps `calc_next_at(_, prev + 1s)` until `source` is no longer `Repetition`, i.e. the next anchor, capped at 100 years; events with no anchor field — no short date or `monthly_pattern` — fall back to a single ordinary dismiss). `set_setting`/`get_setting` pass through to the storage settings table; `last_snooze(chat_id) -> i64` reads `LAST_SNOOZE_SETTING` with a `view::DEFAULT_SNOOZE_MINUTES` fallback (infallible, display-only). Morning digest: `digest_time` → `Option<NaiveTime>` (chat-local; `None` = off), `set_digest_time[_at]` (a time already past today stamps `DIGEST_SENT_SETTING` = today so the first digest waits for the next morning), `collect_due_digests_at(now)` builds the due digests — for each enabled+activated chat past its local digest time and unhandled today, page 0 of the `/today` list (`ListKind::Today` rendering + the `view::DIGEST_NOTE` "sent because the morning digest is on → /settings" footer + `td` pagination keyboard, so page turns reuse `handle_list_callback` — a page turn re-renders as the plain /today list, dropping the footer) and stamps the day (empty days stamp but send nothing; per-chat errors logged, not fatal). `activate_chat(chat_id) -> bool` idempotently sets `activated=true` and reloads the next-event cache (the chat's pre-existing events become fireable); `true` = newly activated. Timezone: scheduler calls resolve the chat `Tz` **internally** from `event.chat_id` (batch paths memoize per-chat); `get_timezone` → `Option<Tz>` (None drives the scheduling gate), `tz_or_utc` (display fallback), `set_timezone(chat_id, tz) -> usize` upserts the setting and re-anchors the chat's active events via `tz::shift_wallclock` (ids collected first — rescheduling mutates the active predicate under LIMIT/OFFSET; `in_offset` events skipped; a shifted occurrence in the past is `calc_next_at`-recomputed forward), returning the re-anchor count.
- **commands/** — One module per command (or group). `mod.rs`: `Command` (`/help`, `/events`, `/today`, `/tomorrow`, `/week`, `/month`, `/timezone`, `/settings`, admin `/import <user_id> <timezone>`/`/database`/`/logs`, hidden `/start`/`/exit`), shared `CmdContext`, `Command::handle` dispatch, `pub use` re-exports (flat `commands::X` paths).
  - **list.rs** — paginated list commands. `fetch_page(kind, provider, chat_id, tz, page)` returns one page + total (paging in storage; Today/Tomorrow/Week/Month windows are the chat's **local** calendar days converted to UTC bounds via `day_window`); `handle_list`/`handle_list_callback` edit in place with `view::list_keyboard`; a page-turn to a vanished page clamps to the last page and refetches. `ListKind` itself (tags/titles/row styles) lives in `view/list.rs`.
  - **event.rs** — single-event view `/event<id>` (matched manually in `main` via `parse_event_command`, not in the menu): `handle_event_view` → `view::event_detail` + `view::event_actions_keyboard` (`⏭ Dismiss` only when active; `⏩ Dismiss repetition` only when active **and** `source` is `Repetition`). Callbacks decoded by `parse_event_callback`, dispatched by `handle_event_callback`: `dis` dismiss, `disr` dismiss-repetition, `sn` snooze, `snx` expand the collapsed snooze row, `del`/`delyes`/`delno` delete, `ed`/`edno` edit. Dismiss variants delegate to `EventProvider` and re-render the detail in place; delete/edit prompts edit keyboard/message in place. Edit on a snoozed event redirects the flow to its parent (`resolve_edit_target`); Delete removes only the pressed event, while deleting a parent cascades to its snoozes via the FK. The `:n`-suffixed variants (`dis:n`/`disr:n`/`del:n`/`delyes:n`/`delno:n`) are the notification-keyboard flavor — they refresh the fired message instead of swapping in the detail view: dismiss re-renders the fired text with a fresh launches preview (`view::dismissed_notification_text`) plus `view::notification_keyboard` (collapsed, with the chat's `provider.last_snooze`) from the advanced schedule (via `refresh_dismissed_view`), delete-confirm clears markup (toast only), delete-cancel keeps the text and restores the collapsed notification keyboard from the stored event's state.
  - **snooze.rs** — the `sn` buttons: inserts a one-off child event whose `parent` is the original's root (`parent.unwrap_or(id)` — snoozes never chain), owning only its time (empty stored message, parent's `msg_id` reused; the text resolves from the parent, so edits propagate); `next_datetime`/`created_at` are UTC while the child's `date`/`time` carry the chat-local reading (consistent with edit prompts and tz-change reschedules). A successful snooze upserts the picked minutes into `LAST_SNOOZE_SETTING` (failure logged, never fails the snooze). Deliberately **not** gated on an unset timezone (pure-instant). Confirms with `view::snoozed_message` + `event_actions_keyboard` from the reloaded event. Also `handle_snooze_expand` for `eid:<id>:snx` — access-checks and swaps the collapsed notification keyboard for `expanded_notification_keyboard` in place (fired text kept).
  - **timezone.rs** — `/timezone` (current setting + region picker) and `handle_timezone_callback` for the `tz:` envelope: `tz:r`/`tz:g:<Region>:<page>` edit only the picker keyboard in place; `tz:p:<Zone>` calls `provider.set_timezone`, toasts, and replaces the message with `view::timezone_set_message`.
  - **settings.rs** — `/settings` (`handle_settings`, opens the menu as a new message) and the `st:` Settings-menu callbacks: `o` opens the menu as a **new** message (the entry button under the /events list keyboard and the /help reply — the pressed message stays intact; shares `send_settings_menu` with the command), `s` re-renders it in place (the picker's Back), `on`/`off` toggle the morning digest (`on` uses `view::DEFAULT_DIGEST_HOUR`), `t` swaps in the hour picker, `h:<H>` sets the digest hour via `provider.set_digest_time`. Menu/picker rendering lives in `view/settings.rs`.
  - **cancel.rs** — `pm:` Cancel of the time-only flow. **start.rs** (welcome: `view::welcome_message`), **help.rs** (attaches `view::settings_entry_keyboard`), **import.rs**, **database.rs**, **logs.rs**, **exit.rs** — one file per remaining command.
- **converter.rs** — Pure. Legacy MateBot `.alert` files (`OLD-SPEC.md`) → `EventInfo` (`legacy=true`). `convert(…, now, tz)` takes UTC `now`/`created_at` and the target chat's `Tz` (legacy wall-clock fields are chat-local; epoch-ms `lastActivePeriodTime` converts via `ms_to_naive_utc`). Future `lastActivePeriodTime` used directly; stale rolled forward; else `calc_next_at`. Unparsable inputs kept as inactive raw-text events.
- **import.rs** — Admin `/import` orchestration. `PendingImport` holds target chat **+ `Tz`** between command and zip (`/import <user_id> <timezone>`; both args required, tz via `tz::parse_tz`, bad args → usage reply); `import_zip(…, tz)` reads all legacy wall-clock data in that tz (the `.alert` filename stamp converts to UTC through it), persists it as the chat's timezone setting **only when the chat has none** (existing settings never overwritten), converts each entry via `insert_prebuilt_event`, returns counts + HTML report (rendered in the same tz).
- **pending.rs** — In-memory flow state only: `PendingMessage` (chat→body-less `EventInfo`, time-only flow) + `PendingEdit` (chat→event id, edit flow). Their prompt strings and `cancel_keyboard` live in `view/prompt.rs`.
- **view/** — The presentation layer (see Invariants): all output HTML + inline keyboards; every time-bearing helper takes `loc` **and the chat `Tz`** (instants arrive UTC and convert at the formatting boundary). `mod.rs` re-exports everything flat (`view::X`); shared `test_support::sample_event` for the co-located tests.
  - **message.rs** — text primitives: `format_when`, `html_to_plain`/`message_preview` (strip+truncate, locale-free), the composed one-liners `unparsable_message`/`inactive_event_reply`/`user_registered_message` (admin new-user notice), `welcome_message` (the /start reply: description + example + /help pointer), `help_message` (the /help reply: escaped command list + tap-to-copy `<code>` examples + admin block), `MESSAGE_TRUNCATED`, `BULLET` (the `▪` list-row marker every bulleted line uses), and the **length clamp**: `rendered_len` (UTF-16 units — how Telegram counts) and `clamp_message` (cap at `MESSAGE_MAX_LEN` = 4096 − reserve, falling back to escaped plain text).
  - **event.rs** — `next_launches_preview` (≤3 upcoming; "" for one-off), the shared detail body (`event_detail` = no caption; `scheduled_message` = same body under a bold `✅ Event created` first line, sent for new parse / time-only / edit completion; `snoozed_message` = ditto with `💤 Event snoozed`, sent by the snooze flow; callers attach `event_actions_keyboard`): bold `Time: <datetime> (relative[, recurrence])` line (no bullet; recurrence via `describe_recurrence` — weekday sets collapse contiguous runs of ≥3 days into full-name ranges like `every Monday-Sunday` via `types::weekday_runs`, the grouping shared with canonical `format_weekday_set`), blank line, full HTML message, launches preview; inactive events render a single bold notice instead of the Time-line/launches — "⌛ Event is out of date. Last fired at <`last_next_datetime`>", or "⌛ Event was dismissed." when `last_next_datetime` is still in the future (dismissed before firing). `event_when_line` (the bulleted `▪ <b>…</b>` variant) serves only the paginated lists' two-line rows. Also `edit_prompt` (re-parseable input: canonical time expression + the original message HTML with formatting intact, selectable for copy-paste), and the event keyboards: `event_actions_keyboard`, `edit_cancel_keyboard`, `delete_confirm_keyboard`.
  - **list.rs** — `ListKind` (tag/title/empty/row-style per list; fetching stays in `commands::list`), `format_page_at` (renders an already-fetched page slice + total, rows separated by a blank line; `RowStyle` — `TwoLine`/`PreviewLink` — picks per-row layout; every user-typed list (`/events`/`/today`/`/tomorrow`/`/week`/`/month`) → two-line, hidden `Missed` kind (`ms` tag) → `missed-datetime — preview /event<id>` rows (absolute datetime only, no relative part; the snapshot carries the missed moment in `next_datetime`)), `list_keyboard` (`◀ Prev` / `<page>/<total>` no-op / `Next ▶`; the `Events` kind additionally always carries the `⚙ Settings` entry row, so `/events` always has a keyboard), `format_missed_page` (page 0 of the startup missed send), `total_pages`, `LIST_PAGE_SIZE`.
  - **notification.rs** — fired-reminder presentation: `fired_message(event, now, due, post_fire_active, post_fire_is_repetition, last_snooze, tz, loc) -> TgMessage` (text = `<message><preview>\n\n<SNOOZE_HINT>`) and the two keyboard flavors: pub `notification_keyboard(id, active, is_repetition, last_snooze)` — the **collapsed** default, one snooze row `Snooze for ...` (`eid:<id>:snx`, expands in place) + last-used shortcut `Snooze <label>` (`eid:<id>:sn:<last_snooze>`; label via `SNOOZE_OPTIONS` lookup, `<N> mins` fallback) — and pub `expanded_notification_keyboard(id, active, is_repetition)` — the full two snooze rows (`eid:<id>:sn:<minutes>`), swapped in by the `snx` handler. `dismissed_notification_text(event, now, tz, loc)` rebuilds the fired text after a dismiss — body + preview led by the new `next_datetime` (baseline `next − 1s`), used by `refresh_dismissed_view`. Both share the action rows: a dismiss row when the event stays active post-fire — `eid:<id>:dis:n`, plus `eid:<id>:disr:n` when the upcoming `source` is `Repetition`; plus an Edit/Delete row — `eid:<id>:ed` / `eid:<id>:del:n`. `DEFAULT_SNOOZE_MINUTES` (= 5) is the last-used fallback for chats that never snoozed.
  - **prompt.rs** — pending-flow prompts (`ASK_TEXT`, `EDIT_ASK_TEXT`, `EDIT_NEED_TEXT`, `EDIT_NEED_TIME`) and `cancel_keyboard` (`CANCEL_DATA` = `pm:cancel`).
  - **settings.rs** — the Settings menu (`settings_message`/`settings_keyboard`: morning-digest state + on/off toggle + time button while on), the digest hour picker (`digest_time_keyboard`, 24 whole hours + Back, `DIGEST_TIME_ASK`), the `⚙ Settings` entry (`settings_entry_row` shared with the /events list keyboard, `settings_entry_keyboard` for /help, `SETTINGS_OPEN_DATA` = `st:o`), `DIGEST_NOTE` (the digest footer pointing at /settings), `DEFAULT_DIGEST_HOUR` (= 8, the on-toggle default). Flow wording, not time vocabulary — no `LocaleProvider` methods.
  - **timezone.rs** — the two-step timezone picker: `timezone_regions_keyboard` (nine continental prefixes, 2/row, `tz:g:<Region>:0`, pinned UTC row `tz:p:UTC`; deprecated/`Etc/` zones not offered but still storable), `timezone_cities_keyboard(region, page)` (16 zones/page from a `LazyLock` grouping of `TZ_VARIANTS`, pick = `tz:p:<full IANA name>`, nav row + `« Regions`), the flow texts `TZ_ASK`/`TZ_REQUIRED`/`timezone_current_message`/`timezone_set_message` (flow wording, not time vocabulary — no `LocaleProvider` methods). A test pins every generated callback ≤ 64 bytes.
- **main.rs** — Entry point + teloxide `Dispatcher`. Startup first takes an exclusive advisory lock on `/tmp/perbot.lock` (`acquire_instance_lock`, held for the process lifetime; our PID is written into the file, and a second instance logs an error with the holder's PID and exits). It then clears stale command scopes and `set_my_commands`; a failing `provider.start(msg_tx)` notifies the admin and returns before the dispatcher is built. `message_handler` + `callback_handler` (routes `eid:`/`pm:`/`tz:`/`st:`/list — the `tz:` and `st:` branches must stay explicit; the list fallback swallows unknowns) return `anyhow::Result` and are wrapped by `*_safe` endpoints that catch errors, tell the user "Something goes wrong!", and forward detail to the admin. Right after the chat upsert, the **activation hook**: `provider.activate_chat` on every message; a fresh registration notifies the admin via `view::user_registered_message` (skipped when the chat *is* the admin; activation errors are logged, not fatal — the next message retries). Before any further message handling (after only the activation hook and the admin import-zip hook), a **timezone gate**: a chat with no `timezone` setting gets `view::TZ_REQUIRED` + the region picker instead of anything else — commands included — and both pending flows are cleared; only the `tz:` picker callbacks get a chat past it. Sole carve-out: `/start` sends `view::welcome_message` before the picker (a new user's first contact reads welcome → timezone prompt). Holds the two text-completion flows: **time-only** (`PendingMessage` → render body → schedule) and **edit-completion** (`PendingEdit` → re-parse, copy identity fields, `update_event_and_get`). Applies `clamp_message` to reminder bodies at ingestion, warning with `view::MESSAGE_TRUNCATED`. Every scheduling path confirms via `send_schedule_confirmation` (`view::scheduled_message` + `event_actions_keyboard`, or `inactive_event_reply`). Sender task is a dumb HTML pump.
- **tgbot.rs** — `TgBot`, the `Clone` logging wrapper every outbound call uses (see Invariants): `send_html`/`send_text`, `send_markdown`, `edit_html`/`edit_text`/`edit_markup`, `answer_callback` (`Option<text>` toast), `send_document` (`&Path` + optional name), `get_file`/`download_file`, `get_me`, `set_my_commands`/`delete_my_commands`.
- **logger.rs** — `init()` sets up `flexi_logger` (daily rotation to `LOG_DIR` + stdout); `current_log_path`.
- **bin/bench.rs** — Storage throughput benchmark.

## EventInfo fields

`EventInfo` (`types.rs`) carries one reminder end to end. Parser sets the first group; storage/scheduler fill the rest. Time/recurrence fields are largely mutually exclusive.

| Field | Type | Set by | Notes |
|-------|------|--------|-------|
| `date` | `Option<NaiveDate>` | parser | **Chat-local** wall-clock. Short date (no year) → **yearly** (unless a non-year `repetition` makes it the start anchor). Full date → one-off (unless `every [N] year[s]`). |
| `time` | `Option<NaiveTime>` | parser | **Chat-local** wall-clock. Clock time anywhere. Absent for `in_offset`/`bare_hour`. |
| `year_explicit` | `bool` | parser | `true` only when a full date spelled the year — honors `date`'s year vs rolling it forward yearly. |
| `days` | `Option<HashSet<Weekday>>` | parser | Weekday-set recurrence; pairs with `years`. |
| `years` | `Option<HashSet<i32>>` | parser | Standalone year token(s) 2000..=2100; restricts a `days` schedule. |
| `repetition` | `Option<Repetition>` | parser | `every <n> <unit>` interval. On a short date, a non-year repetition fills between the yearly anchors (which keep priority); a year-unit one is dropped as redundant. |
| `in_offset` | `Option<(u32, TimeUnit)>` | parser | Relative offset (`now + offset`); with `repetition` repeats. Exclusive with `time`/`date`. |
| `bare_hour` | `Option<u32>` | parser | Leading bare hour 0..=24 → next occurrence of that hour. |
| `monthly_pattern` | `Option<MonthlyPattern>` | parser | Ordinal weekday / last day / fixed `DayOfMonth`. With a `repetition`, the day-of-month anchor has priority. |
| `message` | `String` | parser → `main` | Body as **HTML fragment** (see Invariants). |
| `id` | `i64` | storage | PK; `0` before insert. |
| `chat_id` | `i64` | storage/caller | Destination chat. |
| `active` | `bool` | scheduler | `true` while a future occurrence remains. |
| `next_datetime` | `Option<NaiveDateTime>` | scheduler | Next fire, **UTC**; `None` → inactive. |
| `source` | `Option<NextSource>` | scheduler | Which field produced `next_datetime` (`Time`/`Date`/`BareHour`/`InOffset`/`Repetition`/`MonthlyPattern`/`Years`/`Weekdays`). `Some` iff `next_datetime` is `Some`. Persisted. |
| `last_next_datetime` | `Option<NaiveDateTime>` | scheduler | Most recent non-null `next_datetime` (**UTC**); retained when inactive (drives the "out of date / last fired at" notice). |
| `created_at` | `NaiveDateTime` | storage/converter | Insertion time, **UTC** (legacy: from `.alert` filename, converted from the chat's tz). |
| `msg_id` | `i64` | storage/caller | FK to originating `messages` row. |
| `legacy` | `bool` | converter | Imported from legacy `.alert`. |
| `parent` | `Option<i64>` | snooze flow | Id of the snoozed original (always the root — snoozes never chain); `is_snoozed()` = `parent.is_some()`. The child owns only its time: `message` resolves from the parent at query time, and the child is cascade-deleted with it. |

## Test Cases

`test-cases.md` holds markdown tables driving `tests/table_tests.rs`. Rows alternate `USER` (parse + `insert_event_and_get_at`) and `SYSTEM` (`update_at_and_reload`, assert `next_datetime` or `NONE`). A USER Input starting with `!` is an **action row**: `!Dismiss` / `!Dismiss repetition` (case-insensitive) call `EventProvider::dismiss` / `dismiss_repetition` on the current event (columns 4–5 empty; the timestamp is unused — both advance from stored `next_datetime + 1s`); any other `!command` fails the table. Column 4 by actor: USER rows = expected `event.message`; SYSTEM rows = expected source as `source=<name>` (empty on `NONE` rows). Column 5 = expected `normalize_time()` (USER rows; empty when input doesn't parse). A literal `\n` in Input/Message decodes to a real newline. Add scenarios by appending `###` sections — no code changes needed. Tables run **timezone-neutral**: the test chat has no `timezone` setting, so everything resolves to UTC and each timestamp is chat-local and UTC at once; short-date year inference uses the row's own timestamp (`parse_at`), not the wall clock.

## Datetime formats

- `13:23`, `5:24 PM`, `1:23 26.11`, `31.12.2027` — clock time anywhere; bare hour / offset / short date must lead. Minutes accept 1-2 digits (`10:6` → `10:06`).
- **Short date, no year** (`10:03 15.12`) → **yearly**; redundant `every year`/`yearly` absorbed (canonical `10:03 15.12 yearly`). **Full date + `every [N] year[s]`** → true yearly repetition (first fire on the date). **Full date alone** → one-off.
- **Short date + non-year repetition** (`11:07 05.11 every 2 days`) → date is the start anchor; the repetition fills between fires, but the **yearly date anchor has priority** (interval steps never skip it). Canonical keeps trailing `yearly`: `11:07 05.11 every 2 days yearly`.
- `13:45 mon-fri`, `13:25 thu-fri,sun 2023` — weekday sets, optional year. Leading `every` absorbed.
- `14:55 20.05 every 2 weeks`, `15:30 every 3 days` — start datetime then interval.
- `8 call Alex` → next 08:00; `24` → 00:00; `25` → invalid.
- `8 min call her`, `in 8 min every 2 hours test` — relative offset, optionally repeating; leading `in` absorbed (and canonical).
- `10:00 first sunday`, `17:00 3rd friday` — ordinal weekday (`1st`–`5th`, `last`) of the month; an optional `of [the] month` tail is absorbed.
- `18:00 last day of the month`, `18:00 last day of month`, `18:00 last day` — last day of month.
- `12:05 28th of the month`, `every 28 of the month`, `each 5 of the month` — fixed calendar day (`1`–`31`); `of [the] month` required, optional `day`/ordinal/`every`/`each` absorbed. Missing days (Feb 31) skipped. Combinable with an interval (anchor has priority). Canonical `each <N><ord> day of the month`.
